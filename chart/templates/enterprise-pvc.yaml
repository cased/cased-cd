{{- if .Values.enterprise.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "cased-cd.fullname" . }}-enterprise-audit
  labels:
    {{- include "cased-cd.labels" . | nindent 4 }}
    app.kubernetes.io/component: enterprise
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.enterprise.persistence.storageClass }}
  storageClassName: {{ .Values.enterprise.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.enterprise.persistence.size }}
{{- end }}
